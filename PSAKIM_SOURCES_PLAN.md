<div dir="rtl">

# תוכנית הרחבת מאגר פסקי הדין ומראי המקומות

נכתב ב-20.9.2026 אחרי בדיקה של המערכת הקיימת, של מצב הנתונים במסד, ושל מקורות חיצוניים.

---

## 1. איך עובדת מערכת ההורדה היום

| שלב | קובץ | מה קורה |
|---|---|---|
| סריקת האתר | `scripts/download-all-psakim.mjs` | עובר על מזהים 1–14,500 ב-`psakim.org/Psakim/File/{id}`, השהיה 1.5 שניות, שומר manifest להמשך אחרי עצירה |
| חילוץ תוכן | אותו קובץ, `extractContent()` | כותרת, בית דין, דיינים, מספר תיק, תאריך, גוף הפסק, וגם **עץ המקורות** (`extractTags`) |
| אינדקס מקורות | `scripts/fetch_psakim_index.mjs` | מושך את `PsakimData/Tag?id={leaf}` לכל 3,537 עלי עץ המקורות → `public/tag_psakim_map.json` |
| ייבוא למסד | `scripts/import-psakim-to-db.mjs` | קורא את ה-HTML מ-`all-psakim/`, מכניס ל-`psakei_din`, מדלג על כפילויות לפי כותרת |
| חילוץ מראי מקומות | `supabase/functions/extract-references` | 12 תבניות regex + AI (Gemini) + ניקוד ביטחון → `talmud_references` |
| הרצה בהמונים | `supabase/functions/bulk-analyze` | קבוצות של 5 פסקים, 2 שניות ביניהן |
| קישור לסוגיות | `sugya_psak_links`, `pattern_sugya_links` | קישור פסק ↔ סוגיה להצגה בדף הגמרא |

## 2. מצב הנתונים (20.9.2026)

- **5,093 פסקים**, מתוכם 4,092 עם טקסט מלא; 1,005 כמעט ריקים (989 מהם עם קובץ מקור שאפשר לחלץ ממנו מחדש).
- **psakim.org מוצה**: 2,164 קבצים הורדו, 2,072 כבר במסד. דגימה של 8 מזהים לאורך הטווח החזירה 3 קיימים בלבד.
- **מראי מקומות**: 9,797 שורות. אחרי הייבוא של היום, 43.8% מהפסקים מקושרים לפחות למקור אחד (לפני כן 36.6%).
- **רק 1,866 פסקים (37%) עברו אי פעם ניתוח**. כ-2,200 פסקים עם טקסט טוב מחכים לניתוח ראשון.

### מה בוצע היום
1. **גיבוי מלא** לענן לפני הכל: 41,754 שורות, אומת בבדיקת תקינות.
2. **ייבוא אינדקס המקורות הרשמי** (`scripts/import-source-index-refs.mjs`): +1,287 מראי מקומות מדויקים ברמת עמוד, ו-244 קיימים אושרו. האינדקס הזה היה מונח ב-`public/` מאז הסריקה ולא היה בשימוש.
3. **סימון 262 מראי מקומות שגויים** שמפנים לדף שלא קיים במסכת.

## 3. הבעיה המרכזית שהתגלתה

**ה-AI רואה רק את תחילת הפסק.** `extract-references` שולח `text.slice(0, 6000)` ו-`bulk-analyze` שולח `substring(0, 8000)`, בעוד שאורך פסק ממוצע הוא כ-34,000 תווים. כלומר **כ-80% מכל פסק לא נבדק**.

הראיה: מתוך 1,287 המקורות הרשמיים שייבאנו היום, החילוץ האוטומטי מצא קודם רק **24** ברמת דף. הסיבה הנוספת היא קיצורים: פסק שכותב "ב"מ" או "ב"ב" במקום "בבא מציעא" חומק מרוב התבניות.

## 4. איכות מראי המקומות

| בדיקה | תוצאה |
|---|---|
| מפנים לדף שמעבר לסוף המסכת | 262 מתוך 9,797 (2.7%) — סומנו כשגויים |
| קישורי סוגיה שגויים (`pattern_sugya_links`) | 1,547 מתוך 10,118 (15.3%) |
| מתוכם: מספר שנראה כספירת **עמודים** במקום דפים | 704 |
| מתוכם: מספרים בלתי אפשריים (עד דף 808 בסנהדרין) | 843 — מילים עבריות שנקראו כגימטריה |
| קישורים לדף א׳ (לא קיים, הגמרא מתחילה בדף ב׳) | 27 |

הספירה בתורה היא באותיות (דף ב׳, ג׳, ד׳) ולא במספרים. במסד שמור מספר לצורך מיון, אבל שדה `normalized` מחזיק את הצורה הנכונה (`בבא מציעא ע״ג:`), וזה מה שמוצג למשתמש. הבדיקה מול מספר הדפים במסכת (`maxDaf`) חייבת לרוץ על כל מקור לפני שמירה.

---

## 5. המלצות, לפי סדר תועלת

### א. לתקן את חלון הטקסט של ה-AI — הדבר החשוב ביותר
לחלק את הפסק לקטעים של 6,000 תווים עם חפיפה קלה, לנתח כל קטע, ולאחד. ה-regex ממילא רץ על כל הטקסט, אז העלות היא רק בקריאות ל-AI. בלי זה, כל ניתוח מחדש יחזור על אותו פספוס.

### ב. להשלים ניתוח ל-2,200 הפסקים שלא נותחו מעולם
אחרי תיקון (א). זה מכפיל את הכיסוי בלי להוריד אף פסק חדש.

### ג. להוסיף את שאר עץ המקורות
האינדקס שכבר הורד מכיל גם **4,726 הפניות לשולחן ערוך, 1,327 לרמב"ם ו-96 לירושלמי** שאינן בשימוש. הטבלה `talmud_references` היא לבבלי בלבד, ולכן צריך טבלה כללית (`psak_sources`) עם סוג מקור. זה בדיוק "מראה מקומות מפורט ככל האפשר".

### ד. לנקות את קישורי הסוגיות
למחוק או לסמן את 1,547 הקישורים השגויים, ולהוסיף בדיקת תקינות (`daf` בין 2 ל-maxDaf) לפני כל כתיבה.

### ה. מקורות חדשים להורדה — לפי סדר עדיפות

| מקור | כמה | גישה | מראי מקומות | רישוי | קושי |
|---|---|---|---|---|---|
| **בתי הדין הרבניים ב-gov.il** | 3,437 פסקים | API של JSON, 10 בכל קריאה (~344 קריאות), טקסט מלא בקובצי **docx** | בתוך הטקסט, צפיפות גבוהה | נחלת הכלל לפי סעיף 6 לחוק זכות יוצרים | בינוני — יש שער Cloudflare |
| **ארץ חמדה גזית** (beitdin.org.il) | ~860 פסקי ממונות | HTML רגיל, ~950 בקשות | צפיפות הכי גבוהה, מצטט סוגיות במפורש | כל הזכויות שמורות — צריך לבקש רשות | קל |
| **פד"ר, כרכים א–כב** ב-gov.il | 22 כרכים | PDF סרוק, כרך אחד = 60MB | תלוי באיכות ה-OCR | נחלת הכלל | קשה — לבדוק OCR על כרך אחד לפני התחייבות |
| **din.org.il** | שו"ת "שאל את הרב" | HTML | בתוך הטקסט | לא נבדק | קל, אבל ז'אנר אחר |

**מקורות שנבדקו ונפסלו:** נבו (חוסם מפורשות סורקי AI, בתשלום), ישיבה.org.il (אוסר בקובץ robots), תקדין ופסקדין (בתשלום או ללא תוכן רבני), פרויקט השו"ת של בר אילן ואוצר החכמה (מנוי סגור, בלי ממשק), judgments.org.il (בתי משפט אזרחיים בלבד).

### ו. לאמת כל מראה מקום מול ספריא
`GET https://www.sefaria.org/api/name/<הפניה>` מחזיר הפניה קנונית. נבדק שהוא מזהה גם `ב"מ כא ע"ב` בקיצור, גם `כ"א` עם גרשיים וגם `כא.` בנקודה. תנאי: לקבל רק תשובה שיש בה שדה `ref`. הממשק `find-refs` אינו שמיש כרגע.

### ז. רישוי ותיוג מקור
לכל פסק צריך לשמור את מקורו ואת אופן הייחוס: gov.il נחלת הכלל, psakim.org דורש ייחוס גלוי ושימוש לא מסחרי, ארץ חמדה גזית דורש רשות. לכן צריך שדה ייחוס לכל רשומה ולא הערת שוליים אחת גלובלית.

</div>
