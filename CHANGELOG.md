# יומן שינויים

תיעוד של מה שהשתנה, למה, ואיך לוודא או לחזור אחורה. הרשומה החדשה למעלה.

לתמונה כוללת של הפרויקט — ארכיטקטורה, נתונים, המלצות ומה פתוח — ראו [`AI_HANDOFF.md`](AI_HANDOFF.md).

---

## 2026-09-22 — מילים עברית שהומרו לדפים: הבאג שהרעיל את מראי המקומות

### מה היה שבור

שני החילוצים — הביטויים הרגולריים ב-`extract-references` ו-`textAnalyzer` — סכמו כל אות
עברית שבאה אחרי שם מסכת, בלי לבדוק שהצירוף הוא בכלל מספר. לכן:

| מה שנכתב בפסק | מה שנשמר | למה |
| --- | --- | --- |
| `בבא בתרא דף ב` | בבא בתרא **פ״ד:** | המילה **"דף"** עצמה: ד+ף = 84, והאות ב שאחריה נקראה כעמוד |
| `קידושין בטלים.` | קידושין **צ״א.** | ב+ט+ל+י+ם = 91, והנקודה נקראה כעמוד א |
| `תמורה עליה.` | תמורה **קט״ו.** | ע+ל+י+ה = 115, ובמסכת תמורה יש 34 דפים בלבד |
| `שבת הגזילה, א` | שבת **ס׳.** | ה+ג+ז+י+ל+ה = 60 |
| `כתובות דף פ״ד ע״ב` | כתובות **פ׳** (בלי עמוד) | אסימון הדף נעצר בגרשיים, ורק האות פ נקראה |

**755 מראי מקומות** במסד היו מילים שהומרו לדפים, ועוד **857** היו מספרים שנקטעו.

### התיקון

מספר עברי נבדק עכשיו לפי **צורה** ולא בסכימה: מאות בסדר יורד (ת״ק, תת״ק), עשרה אחת,
יחידה אחת, ט״ו/ט״ז ל-15/16, וכל תו שנשאר בלי שימוש פוסל את המחרוזת. `דף`, `עליה`,
`בטלים`, `שהד` — כולן מחזירות null. בנוסף:

- **הדף חייב להתקיים במסכת**, ולא רק להיות קטן מ-200. הבדיקה הזו הוחלה גם על מה שה-AI מחזיר.
- **אסימון הדף שומר את הגרשיים הפנימיים**, כך ש-`פ״ד` הוא 84 ולא 80.
- התבנית שבלי עמוד אינה יכולה עוד להתאים לתחילתו של מספר ארוך (`דף פ״ד` אינו דף פ׳).
- בתבנית רופפת (בלי "דף" ובלי סימון עמוד) האסימון מוגבל לארבע אותיות.

החילוץ עבר ל-`supabase/functions/_shared/extractRegex.ts` כדי שיהיה **ניתן לבדיקה
מקומית**, ונוספו 30 בדיקות: הפורמטים האמיתיים, המילים שנקראו בעבר כדפים, וגבולות המסכת.

### הנתונים אחרי התיקון

| פעולה | כמה | הכלי |
| --- | --- | --- |
| מילים שהומרו לדפים — נמחקו | 755 | `scripts/triage-bad-references.mjs` |
| מספרים שנקטעו והוחלפו בקריאה השלמה | 1,102 | `scripts/extract-refs-local.mjs` |
| מראי מקומות חדשים מחילוץ מתוקן | 6,856 | `scripts/extract-refs-local.mjs` |
| סומנו כשגויים על בסיס הכרעה לא תקפה — הוחזרו לבדיקה | 70 | `scripts/triage-bad-references.mjs` |
| מחלוקות אמיתיות שנשארו לתיקון ידני | 29 | |

מחיקות נשמרות לקובץ JSON תחת `scripts/data/` לפני הביצוע.

### ירושלמי ומשנה שנרשמו כדפי בבלי

פסק שכל אזכור התלמוד שבו הוא "(ירושלמי ברכות פ״א ה״א)" הופיע באפליקציה תחת **ברכות ב׳
ע״א** של הבבלי. לירושלמי, למשנה ולתוספתא יש אותם שמות מסכתות, אבל הם מחולקים לפרקים
והלכות — לא לדפים. ה-AI מיפה את הציטוט לבבלי ובחר את הדף הראשון של המסכת, והביטוי
הרגולרי עשה את אותו דבר עם "בירושלמי (סוכה ג,א)", שבו המספרים הם פרק והלכה.

שני החילוצים בודקים עכשיו מה כתוב **לפני** הציטוט ולוקחים את הסימן הקרוב ביותר:
"ירושלמי" פוסל, "גמרא" או "בבלי" לא — כך שב-"גמרא בקידושין כט ע״א, ירושלמי בקידושין"
ההפניה לבבלי נשמרת. צורת פרק־והלכה ("פ״י מ״א") לעולם אינה נקראת כדף.

**69 הפניות הועברו** ל-`psak_sources` תחת הקורפוסים ירושלמי ומשנה, עם הפרק וההלכה
האמיתיים, כך שהציטוט נשמר ולא נזרק (`scripts/move-nonbavli-references.mjs`).

### שתי צורות ציטוט שלא נקראו כלל

- **מסכת ומספר עם גרשיים, בלי "דף" ובלי עמוד** — "מסנהדרין כ״ט", הצורה הנפוצה בכתיבה
  רבנית. הגרשיים נדרשים, כדי שמילה רגילה לא תיקרא כדף.
- **ציטוט שנפתח בסוגריים** — "קידושין (דף ל״א עמ׳ א׳)". קודם לכן נדרש רווח בין שם
  המסכת למספר, והסוגר שבר את ההתאמה.

### חילוץ מקומי, בלי Edge Function

`scripts/extract-refs-local.mjs` מריץ את אותם ביטויים רגולריים מקומית: אין מגבלת 6,000
תווים, אין עלות, ואין תלות בגרסה שמותקנת בענן. 8,541 פסקים נסרקים בכ-2 דקות.

### ייחוס ואימות — שתי עמודות שונות

סקריפט האימות מול ספריא כתב `source='sefaria-verified'` ובכך **מחק את הייחוס** מי חילץ את
ההפניה. נוספו `validated_by` ו-`validated_at` (מיגרציה `20260921100000`), האימות נכתב שם,
והייחוס שנמחק שוחזר מהגיבויים: 7,392 שורות חזרו ל-`regex`/`ai`, ו-9,057 שנוצרו אחרי
הגיבוי האחרון סומנו `extracted` — חילוץ אוטומטי שאי אפשר לדעת אם הגיע מהביטוי או מה-AI.

הסינון במסך האינדקס מציג עכשיו כל מקור בנפרד, כולל **אינדקס האתר** (המדויק מכולם)
ו**חילוץ אוטומטי**. לפני כן כל מה שאינו regex הוצג כ-AI.

### אימות ספריא

- ריצה מלאה: **16,297 אושרו, 152 נסתרו, 2,834 לא זוהו**.
- שתי תקלות באימות עצמו תוקנו: ציטוט שאינו נוקב בשם מסכת (`"צח, ב"`) קיבל מספריא את
  המסכת של השורה הקודמת באצווה, ומחרוזת `raw` משובשת נשפטה במקום ההפניה. שתיהן מדולגות
  עכשיו ומדווחות בנפרד.

### daat.ac.il

2,591 פסקים הורדו, **1,552 יובאו**, ו-1,041 נחסמו כי טביעת האצבע של התוכן זיהתה שהם כבר
במסד. סך הפסקים: **8,541**.

---

## 2026-09-21 — מקורות חדשים, מרשם מקורות, וניקיון

### מקורות פסקי דין

| מקור | נוסף | איך |
| --- | --- | --- |
| בתי הדין הרבניים (gov.il) | 1,984 | API של JSON + חילוץ טקסט מ-docx. 712 מתוך 2,696 שהורדו כבר היו במסד ודולגו |
| בית דין לממונות משפט צדק | 27 | HTML, עם הערות השוליים בנפרד — שם נמצאים הציטוטים |
| בית הדין דאמריקה | 25 | PDF עם שכבת טקסט. חילוץ נאיבי מחזיר ג'יבריש כי הגופנים מוטמעים בקידוד פנימי, ולכן נוספה `pdfjs-dist` שמפענחת ToUnicode |
| daat.ac.il | בהורדה | כ-2,600 פסקים עם מספרי תיק. האתר ב-windows-1255 וחייב פענוח מפורש |

כל פסק מסומן ב-`source_key`, וכל מקור נרשם ב-`psak_source_registry` עם הרישוי והייחוס
שלו. ברשימת הפסקים יש סינון "מקור", ובתפריט ההגדרות יש מסך שמאפשר לכבות מקור מהתצוגה.

### מראי מקומות

- יובא **אינדקס המקורות של psakim.org** — תיוג ידני של מסכת, דף ועמוד, שהיה מונח בריפו ולא היה בשימוש. +1,290 מראי מקומות מדויקים, ועוד 3,394 לשולחן ערוך, רמב"ם וירושלמי בטבלה חדשה `psak_sources`. **בלי AI.**
- **תוקן פספוס עקרוני:** ה-Edge Function שלח ל-AI 6,000 תווים בעוד שפסק ממוצע הוא כ-34,000. הניתוח מתבצע עכשיו בקטעים חופפים, באפליקציה (`src/lib/references/extractAll.ts`) ובסקריפט (`scripts/reanalyze-refs.mjs`). התוצאה: +11,400 מראי מקומות.
- נוספה טבלת `masechtot_daf_limits` וטריגר שחוסם דף שאינו קיים במסכת. 1,574 קישורי סוגיה שגויים נמחקו, 262 מראי מקומות סומנו כשגויים.

### עיצוב וכפילויות

- 4,390 פסקים עוצבו בתבנית הבית דרך `toHouseStyledHtml`, עם הטקסט המקורי שמור ב-`original_text` ואפשרות `--revert`.
- 140 פסקים כפולים אוחדו (`scripts/dedupe-psakim.mjs`), אחרי העברת כל מה שמקושר אליהם.
- נוספה `content_print` — טביעת אצבע של התוכן שמזהה כפילות גם כשהכותרת שונה.

### גיבוי

מערכת גיבוי ושחזור מלאה (`src/lib/backup/`, מיגרציה `20260918120000`): גיבוי לענן או ל-ZIP
לפי נושאים, שחזור בשלושה מצבים, גיבוי ביטחון לפני שחזור, בדיקת תקינות וגיבוי אוטומטי שבועי.

### ניקיון

- `.env.supabase` הוצא ממעקב הגיט (הכיל `VERCEL_OIDC_TOKEN`) ונוסף ל-`.gitignore`.
- נמחקו `-w` ו-`vite.config.ts.timestamp-*.mjs`.
- **נשאר פתוח:** סיסמת האדמין עדיין בהיסטוריית הגיט של הריפו הציבורי. צריך להחליף אותה.

### איך זה אומת

`npm test` (132 בדיקות), `tsc --noEmit`, `npm run build`, ובדיקה ויזואלית בדפדפן של מסך
המקורות ושל פסקים מעוצבים משני מקורות שונים. הייבוא והעיצוב נבדקו קודם על מדגם קטן.

---

## 2026-09-21 — צפיין אחד לכל המסמכים

**PR [#2](https://github.com/ticnutai/gemaraca-463c22f4/pull/2)** · קומיט מיזוג `cfb81d6` · נקודת שחזור: `backup/main-before-pr2-2026-09-21`

### למה

היו באפליקציה **ארבעה** צפיינים שונים לאותו פסק דין, ותפריט שביקש מהמשתמש לבחור ביניהם:

| מה | קובץ | איפה נקרא |
| --- | --- | --- |
| "צפיין רגיל" | `PsakDinViewDialog` (1,700 שורות) | 12 מקומות |
| צפיין EmbedPDF | `EmbedPdfViewerPage` | 8 מקומות |
| "PDF מוטמע" / "EmbedPDF (pdfium)" | `EmbeddedDocViewer` (650 שורות) | מקום אחד |
| "EmbedPDF (pdfium)" בלשונית המסמכים | `PdfViewerTab` | היה בעצם `<embed>` של הדפדפן — שם מטעה |

הלוגיקה הוכפלה, והמשתמש נדרש להחלטה שאין לו סיבה להתעניין בה.

### מה נעשה

**נקודת כניסה אחת.** פסק דין נפתח רק דרך `useDocumentViewer().open(psak)`
(`src/components/DocumentViewerProvider.tsx`), שמציג את `EmbedPdfViewerPage`:

- **בחלון מעל הדף הנוכחי** — העמוד עצמו במצב `embedded=1` בתוך iframe, אותו מנגנון שסריקות
  הש"ס כבר השתמשו בו. הרשימות והאינדקסים מאחור שומרים גלילה, סינון ומצב.
- **או בעמוד מלא** — כפתור "פתח בעמוד מלא", ו-"חזור" חוזר לדף שממנו באת (קודם הוביל תמיד לדף הבית).
- עריכת פרטי הפסק בתוך החלון מודיעה לדף המארח (`postMessage`) והרשימות מתרעננות.

**נמחקו** `PsakDinViewDialog`, `EmbeddedDocViewer`, `ViewerPreferenceDialog`, כפתורי "החלף צפיין"
בשבעה רכיבים, וה-`<embed>` שהתחזה ל-EmbedPDF — בסך הכול כ-1,900 שורות פחות.

**מה שהיה ייחודי לצפיין הרגיל עבר לצפיין המאוחד:** ייצוא Word אמיתי (`.docx` דרך
`src/lib/docxGenerator.ts`) במקום HTML בתחפושת `.doc`. עריכת המטא-דאטה כבר הייתה בעמוד.

### שדרוג המנוע

`@embedpdf/*` **2.8.0 → 2.15.1**, וכל ההגדרות שאינן ספציפיות למסמך רוכזו ב-`src/lib/embedPdfConfig.ts`:

| | לפני | אחרי | למה |
| --- | --- | --- | --- |
| WASM (4.6MB) | jsDelivr | ה-build שלנו, בכתובת אבסולוטית | עובד offline (PWA); ה-worker נטען מ-blob ולכן כתובת יחסית נשברת |
| ממשק הצפיין | אנגלית | עברית — 331 מחרוזות ב-`embedPdfHebrewLocale.ts` | לספרייה אין locale RTL; נרשם בזמן ריצה דרך `onReady` כדי לא לדרוס את האנגלית כ-fallback |
| גופן הממשק | Open Sans מ-Google Fonts | גופן האתר | בלי בקשה חיצונית |
| חותמות | מניפסט מ-jsDelivr | `stamp.manifests: []` | אפס בקשות לצד שלישי |
| בחירת טקסט במגע | לא עבדה | `pan.defaultMode: "never"` | [embedpdf#706](https://github.com/embedpdf/embed-pdf-viewer/issues/706) |
| iOS + סריקות גדולות | קריסה/רענון | `devicePixelRatio` מוגבל ל-1.5 בעמוד הצפיין | [embedpdf#692](https://github.com/embedpdf/embed-pdf-viewer/issues/692) |
| השחרה | פעילה | מושבתת | מוחקת טקסט מה-PDF לצמיתות; לא כלי לימוד |

השדרוג גם מביא טפסים, חותמות, חתימות, הערת callout, ייבוא/ייצוא הערות, רינדור מהיר יותר
ו-`setSelection()` (בחירת טקסט תכנותית — שימושי לקישור מגמרא לפסק בעתיד).

### איך זה אומת

ריצה בדפדפן (Chromium, דסקטופ + Pixel 7) על ה-build, מול API מדומה ומול **PDF עברי אמיתי**
שנוצר מתבנית הבית:

- פתיחה מרשימת הדף → חלון → 5 עמודים מרונדרים; "עמוד מלא" → מרונדר; "חזור" → חוזר לסוגיה
- חיפוש "הסדר" בתוך ה-PDF דרך ממשק הצפיין → 11 תוצאות מודגשות
- אפס בקשות לצד שלישי, אפס שגיאות קונסול
- 132 בדיקות יחידה, `tsc --noEmit` נקי, `npm run build` עובר, אין ממצאי lint חדשים

---

## 2026-09-20 — פסקי דין בתוך האתר, התאמה למובייל, תיעוד הענן

**PR [#1](https://github.com/ticnutai/gemaraca-463c22f4/pull/1)** · קומיט מיזוג `df91e18` · נקודת שחזור: `backup/main-before-pr1-2026-09-21`

### 1. פסקי דין מ-gov.il נפתחים בתוך האתר

**הבעיה:** "פתח פסק דין" על פסק של בתי הדין הרבניים הציג מסך ריק; הדרך היחידה לראות אותו
הייתה לפתוח את אתר בתי הדין.

**הסיבה:** הפסקים האלה יובאו עם `full_text` משלהם (הטקסט חולץ מקובצי docx ב-
`scripts/download-govil-psakim.mjs`) **וגם** עם `source_url` לדף ב-gov.il. הצפיין העדיף תמיד את
`source_url`, והדף הזה אינו PDF אלא עמוד אינטרנט ש-gov.il אוסר להטמיע (X-Frame-Options) —
ולכן ה-iframe נשאר ריק.

**התיקון:** כשכתובת המקור היא עמוד אינטרנט רגיל ויש לנו את הפסק במסד — מוצג העותק שלנו.
פסק שכבר עוצב מוצג כמות שהוא; פסק שיובא כטקסט נעטף בתבנית הבית לפני ההצגה. קובצי PDF,
DOCX ותמונות ממשיכים להיפתח בצפיין שלהם, וקישור המקור נשאר זמין.

**מקור אמת אחד לעיצוב:** הלוגיקה עברה ל-`toHouseStyledHtml` ב-`src/lib/psakDinHtmlTemplate.ts`,
והן הצפיין והן `scripts/style/entry.ts` קוראים לה — כך שהתצוגה החיה והפסקים המעוצבים במסד
יוצאים זהים. `scripts/style/build.cjs` נבנה מחדש ופלטו הושווה לגרסה הקודמת: זהה בתו.

בדרך: הורדת HTML חיצוני שנכשלת (CORS) חוזרת לעותק שבמסד במקום להציג שגיאה, ושמירת מסמך
שמוצג מהעותק הפנימי לא דורסת יותר את קישור המקור.

### 2. התאמה יסודית למובייל

נבדק ברוחב טלפון (412px) מסך אחרי מסך, עם בדיקה אוטומטית של גלילה אופקית ואלמנטים שחורגים:

- **צפיין פסקי הדין** — סרגל האייקונים נגלל לרוחב במקום להיחתך; הפאנל הצדדי נפתח כשכבה
  ברוחב המסך במקום עמודה שמעכה את המסמך לרצועה; פיצול/השוואה, הצמדה ומסך מלא מוסתרים
  בטלפון; סרגלי העריכה בשורה נגללת אחת במקום שלוש.
- **תבנית הבית** — `@media` לטלפון: טבלת פרטי התיק נערמת שורה מתחת לשורה במקום עמודת
  תוויות ברוחב 150px (שגרמה לשבירה אות-אות), שוליים צרים, קישור המקור נשבר בתוך המסגרת.
- **דף הסוגיה** — שבע הלשוניות בשורה נגללת בטלפון, רשת בדסקטופ.
- **רשימת פסקי הדין** — הכותרת נשברת לשורות במקום להידחס לעמודה; אייקוני ה-hover (שאין
  להם hover בטלפון) לא תופסים 190px מהכרטיס.
- **כפתורים צפים** — צ'אט ה-AI וחלון הניווט המהיר ממלאים את המסך במקום חלון 400px שיוצא
  ממנו; ריפוד תחתון כדי שהכפתורים לא יסתירו את הכרטיס האחרון.

**באג שנמצא בדרך (גם בדסקטופ):** ב-`TabsContent` מחלקת `flex` גברה על מאפיין `hidden`, ולשונית
לא פעילה השאירה חלל ריק של 200px מעל התוכן. תוקן ברכיב הבסיס `src/components/ui/tabs.tsx`.

### 3. תיעוד הענן והסנכרון עם הדסקטופ

- `CLAUDE.md` — הקשר שנטען בכל סשן של Claude Code: מה הפרויקט עושה, הפקודות, מפת התיקיות
  והמוסכמות.
- `CLOUD_SYNC.md` — איפה הפרויקט שמור (GitHub, Supabase, Vercel, Lovable), איך למשוך אותו
  לדסקטופ ושגרת ה-git לשמירה על סנכרון.

---

## ארכיטקטורת הצפיין היום

```
כל מקום שמציג פסק דין  ──► useDocumentViewer().open({ id, title, source_url })
                                    │
                    ┌───────────────┴───────────────┐
                    ▼                               ▼
        חלון מעל הדף (ברירת מחדל)            עמוד מלא (fullPage: true)
        iframe → /embedpdf-viewer?embedded=1   /embedpdf-viewer?…
                    └───────────────┬───────────────┘
                                    ▼
                        EmbedPdfViewerPage
                    ┌───────────────┴───────────────┐
                    ▼                               ▼
        PDF → מנוע EmbedPDF               HTML/טקסט → תבנית הבית
        (embedPdfConfig.ts)               (toHouseStyledHtml)
```

הכלל רשום גם ב-`CLAUDE.md`: **לא מוסיפים צפיין נוסף.** תוספת יכולת נכנסת ל-`EmbedPdfViewerPage`
או ל-`embedPdfConfig.ts`.

רוב המסמכים באפליקציה אינם PDF: `all-psakim/` הם ~2,000 קובצי HTML, ופסקי gov.il הם טקסט
שחולץ מ-docx. לכן הצפיין הוא מעטפת אחת עם שני מנועי הצגה בפנים.

---

## נקודות שחזור

| ברנץ' | מחזיר את `main` למצב שלפני |
| --- | --- |
| `backup/main-before-pr1-2026-09-21` | PR #1 (פסקי דין בתוך האתר + מובייל) |
| `backup/main-before-pr2-2026-09-21` | PR #2 (צפיין אחד) |

```sh
git checkout main
git reset --hard origin/backup/main-before-pr2-2026-09-21
git push --force origin main
```

או בלי טרמינל: לפתוח PR מהברנץ' הזה אל `main` ולמזג אותו.

---

## פתוח / לא נעשה

- `.env.supabase` עוקב ב-git ומכיל פלט של Vercel CLI כולל `VERCEL_OIDC_TOKEN`. הטוקן
  קצר-מועד, אבל עדיף להוציא את הקובץ מהמעקב ולהוסיף ל-`.gitignore`.
- `npm run lint` מדווח על מאות ממצאים קיימים מראש (רובם `no-explicit-any`).
- קבצי זבל בריפו: `-w` (פלט curl בטעות) ו-`vite.config.ts.timestamp-*.mjs`.
- `EMBEDPDF_SYSTEM_GUIDE.md` מתאר קבצים שנמחקו עוד לפני העבודה הזו (`src/components/book/*`,
  `src/pages/ProPDFViewer.tsx` ועוד). מסומן בראש הקובץ; לא נכתב מחדש.
- העמודה `user_preferences.viewer_mode` נשארה במסד; האפליקציה לא קוראת אותה יותר.
