# סנכרון הפרויקט בין הענן לדסקטופ

המדריך מסביר איך הפרויקט שמור בענן ואיך לעבוד עליו מהמחשב האישי עם Claude, בלי לאבד שינויים.

## איפה הפרויקט שמור

| מקום | מה נמצא שם |
| --- | --- |
| **GitHub** — `ticnutai/gemaraca-463c22f4` | מקור האמת. כל הקוד, המיגרציות, פונקציות ה-Edge, הסקריפטים וכ-2,000 קבצי פסקי דין ב-`all-psakim/` |
| **Supabase** — פרויקט `jaotdqumpcfhcbkgtfib` | מסד הנתונים, האחסון, ההרשאות ופונקציות ה-Edge בפועל |
| **Vercel** | האתר החי (build מתוך ברנץ' ברירת המחדל) |
| **Lovable** — פרויקט `d7f25ac6-…` | עורך ויזואלי; שינויים שנעשים בו נדחפים חזרה ל-GitHub |

כל מה שיש בתיקיית העבודה כאן כבר דחוף ל-GitHub — אין שינויים שלא נשמרו.

## שלב ראשון בדסקטופ: משיכת הפרויקט

```sh
git clone https://github.com/ticnutai/gemaraca-463c22f4.git
cd gemaraca-463c22f4
npm install
npm run dev
```

הריפו כולל היסטוריה ארוכה וקורפוס גדול, כך שה-clone שוקל כ-220MB. לשכפול מהיר יותר:

```sh
git clone --depth 50 https://github.com/ticnutai/gemaraca-463c22f4.git
```

## חיבור Claude במחשב

**Claude Code (CLI או תוסף ל-VS Code / JetBrains)** — הדרך המומלצת לעבודה על הקוד:

```sh
npm install -g @anthropic-ai/claude-code   # התקנה חד-פעמית
cd gemaraca-463c22f4
claude                                      # פותח סשן בתוך תיקיית הפרויקט
```

Claude יקרא אוטומטית את `CLAUDE.md` שבשורש הפרויקט ויקבל את ההקשר — מבנה התיקיות, הפקודות
והמוסכמות — בלי שתצטרך להסביר מחדש בכל שיחה.

**Claude Desktop** — פותחים את אותה תיקייה דרך אפליקציית הדסקטופ; היא מריצה את אותו Claude Code
מעל התיקייה המקומית. אין "סנכרון קבצים" נפרד: הסנכרון הוא git — מה שנדחף ל-GitHub נראה בכל מקום.

**Claude Code on the web / בענן** — סשן כמו זה מקבל clone טרי של הריפו בכל הרצה, ודוחף לברנץ'
ייעודי. לכן הכלל היחיד שחשוב לזכור: *לדחוף ולמשוך דרך GitHub*.

## שגרת עבודה יומית

```sh
git pull origin main          # לפני שמתחילים לעבוד
# …עובדים, עם Claude או בלי…
git add -A
git commit -m "תיאור השינוי"
git push -u origin <branch>
```

- עבודה שנעשית בסשן בענן מגיעה לברנץ' `claude/…` ומשם ל-Pull Request; אחרי מיזוג מושכים `main`.
- שינויים שנעשים ב-Lovable מגיעים ישירות ל-GitHub — כדאי למשוך לפני כל עבודה מקומית.
- אם עבדת במקביל בשני מקומות, `git pull --rebase` יסדר את הסדר לפני הדחיפה.

## משתני סביבה

`.env` מכיל רק את המפתחות הציבוריים של Supabase, ולכן הוא נמצא בריפו ועובר לדסקטופ כמו שהוא.
מפתחות אדמין (service role) שייכים ל-`.env.migrations.local` — הקובץ הזה ב-gitignore ולא עובר
בין מכונות; צריך ליצור אותו מחדש בכל מחשב.

## בדיקה שהכול עובד אחרי המשיכה

```sh
npm test      # 124 בדיקות יחידה — עוברות
npm run build # build מלא — עובר
npm run lint  # מדווח כרגע על 463 ממצאים קיימים (368 שגיאות), לא רגרסיה
```

`npm run test:e2e` (Playwright) דורש דפדפן מותקן — `npx playwright install` בפעם הראשונה.
הסקריפטים `npm run backup:*` כתובים ב-PowerShell ורצים על Windows בלבד.
